public class SlidingWindowTwoPointers {
    // Boilerplate for sliding window
    public static int solve(int[] nums) {
        int left = 0, right = 0;
        int ans = 0;
        while(right < nums.length) {
            // add nums[right] to window
            right++;
        }
        return ans;
    }
}
